# Development Dockerfile
FROM gradle:8.5-jdk17

WORKDIR /app

# Copy gradle files
COPY build.gradle settings.gradle ./

# Copy source code (will be overridden by volume mount)
COPY . .

# Expose Spring Boot port
EXPOSE 8080

# Run bootRun in development mode
CMD ["gradle", "bootRun"]
