<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.synchub.marketly.infrastructure.persistence.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.synchub.marketly.domain.user.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="email" column="email"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findAll" resultMap="UserResultMap">
        SELECT id, username, email, created_at, updated_at
        FROM users
    </select>

    <select id="findById" resultMap="UserResultMap">
        SELECT id, username, email, created_at, updated_at
        FROM users
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.synchub.marketly.domain.user.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, email, created_at, updated_at)
        VALUES (#{username}, #{email}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.synchub.marketly.domain.user.User">
        UPDATE users
        SET username   = #{username},
            email      = #{email},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM users
        WHERE id = #{id}
    </delete>

</mapper>
